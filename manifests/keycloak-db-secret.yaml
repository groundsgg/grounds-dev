# Database Secret for Keycloak
# Contains PostgreSQL credentials for Keycloak database connection
# Credentials match the PostgreSQL configuration in values/postgresql.values.yaml

apiVersion: v1
kind: Secret
metadata:
  name: keycloak-db-secret
  namespace: keycloak
type: Opaque
stringData:
  username: app
  password: app

